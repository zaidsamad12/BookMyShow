postgres=# create database bms;
CREATE DATABASE
postgres=# create user zeesam
postgres-# ;
CREATE ROLE
postgres=# grant all previleges on bms to zeesam
;
postgres=# grant all privileges on database bms to zeesam;
GRANT
